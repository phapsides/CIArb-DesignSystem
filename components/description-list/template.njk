<dl
    class="bc-description-list {%- for v in params.variants %} bc-description-list--{{ v }}{% endfor %} {%- if params.classes %} {{ params.classes }}{% endif %}"
    {%- for attribute, value in params.attributes %} {{ attribute }}="{{ value }}"{% endfor %}>
    {{ caller() if caller }}
</dl>
